((root)
 (rules
   ((keep
      (or ((in-list ("Base" "Core" "Core_kernel"))
           exports-syntax
           exports-modules-only
           (<= scope-lines 40))))
    (remove (<= uses 5))
    (move (>= dist-to-optimal 40))))
 (precedence (keep remove move)))
