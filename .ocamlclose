((root)
 (rules
   ((keep
      (or ((in-list ("Base" "Core" "Core_kernel"))
           exports-syntax
           exports-modules-only
           (<= scope-lines 40))))
    (remove (<= uses 500))
    (to_local false)
    (move false)))
 (precedence (keep remove to_local move)))
