((root)
 (rules
   ((keep
      (or (in-list ("Base" "Core" "Core_kernel"))
          exports-syntax
          exports-modules-only
          (<= scope-lines 40)))
    (remove (<= uses 5))
    (structure (and (<= symbols 5) (>= uses 15)))
    (move (>= dist-to-optimal 40))))
 (precedence (keep remove structure move)))
