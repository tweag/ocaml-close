((rules
   ((keep
      (or ((in_list (Base))
           exports_syntax
           (min_exported 4)
           (min_use 10)
           exports_modules_only)))
    (remove true)
    (convert_to_local false)
    (move false)))
 (precedence (keep remove convert_to_local move)))
