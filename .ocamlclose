((rules
   ((keep
      (or ((in-list ("Base" "Core"))
           exports-syntax
           exports-modules-only
           (<= scope-lines 40))))
    (remove
      (or ((<= symbols 4)
           (<= uses 10))))
    (to_local false)
    (move false)))
 (precedence (keep remove to_local move)))
